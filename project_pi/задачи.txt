Добавить в добавление слов:
удаление слов + вообще до конца прописать логику (активация категории, ограничение по словам, вывод всех слов по нормальному и т.п.)

Добавить саму игру:
логику

Отредактировать setings.json

Сделать ассинхронность с помощью сессий по ip пользователя:

from flask import request

@app.route('/route')
def route():
    client_ip = request.remote_addr
    return f'Запрос получен от IP: {client_ip}'

Добавить условия проверок, что игру нельзя начать, пока не будет команд, категорий слов, настроек игры и т.п.
